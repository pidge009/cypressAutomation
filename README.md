# cypressAutomation


"delete:reports": "rm -r cypress/reports/* || true", // this will only work on Linux or mac 

"delete:reports": "rmdir /s /q cypress/reports/* || true", // this will only work on WIN CP